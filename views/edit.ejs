<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Creative Navbar</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet"> <!-- FontAwesome for icons -->
  <style>
    /* Custom underline animation */
    .nav-link {
      position: relative;
      display: inline-block;
      padding-bottom: 8px;
    }
    .nav-link::after {
      content: '';
      position: absolute;
      width: 100%;
      transform: scaleX(0);
      height: 2px;
      bottom: 0;
      left: 0;
      background-color: green; /* Tailwind Blue 500 */
      transform-origin: bottom right;
      transition: transform 0.25s ease-out;
    }
    .nav-link:hover::after {
      transform: scaleX(1);
      transform-origin: bottom left;
    }
  </style>
</head>
<body class="bg-blue-500">
    <form class="max-w-lg mx-auto flex items-center space-x-2 mt-4" action="/update/<%= post._id%>" method= "Post">
        <textarea 
          class="w-full text-2xl resize-none bg-transparent border-2 border-white rounded-lg text-center text-dark placeholder-gray-200 focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-transparent shadow-lg transition-transform transform " 
          name="content" 
          placeholder="What’s on your mind"
          style="height: 60px;"><%= post.content %></textarea>
        <input 
          class="h-14 w-24 px-4 py-2 text-1xl bg-yellow-400 text-white rounded-lg shadow-lg hover:bg-blue-100 transition-transform transform hover:scale-105" 
          type="submit" 
          value="Update">
    
      </form>

    </body>
